{
  "$schema": "../schemas/agent.schema.json",
  "schema_version": "0.4",
  "agent_id": "AGENT-BUILDER-01",
  "name": "builder-01",
  "role": "builder",
  "runtime": {
    "provider": "opencode",
    "executable": "opencode",
    "version": { "minimum": "1.17.7" },
    "requirements": {
      "runtime_mode": "local",
      "capabilities": ["agent-skill-v1", "execution-manifest-v1", "local-worktree-v1", "rpc-v1", "skill-bundles-v1"],
      "model_strategy": "runtime_default"
    }
  },
  "skills": ["repository-analysis", "git-workflow", "analyze-test-failure"],
  "dependencies": { "system": ["git", "ollama"], "optional": ["docker"] },
  "authentication": { "required": ["github"] },
  "health_checks": ["opencode_cli", "ollama", "github", "repository_access"],
  "responsibilities": ["Iterate on approved engineering tasks with the locally hosted OpenCode model and report execution evidence."],
  "forbidden_actions": ["Merge or approve its own pull request."],
  "inputs": ["engineering_task", "repair_request", "repository"],
  "outputs": ["execution_result", "pull_request"]
}
